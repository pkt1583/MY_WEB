import java.io.IOException;

public class DbUtility {
    public static void main(String[] args) throws IOException {
     /*   Ejb3Configuration configuration1 = new Ejb3Configuration().configure("mysqllocal", new Properties());
        configuration1.buildEntityManagerFactory();
        Configuration configuration=configuration1.getHibernateConfiguration();
        SchemaExport schemaExport = new SchemaExport(configuration);
        schemaExport.setOutputFile("exportDDL.sql");
        schemaExport.execute(true, false, false, true);*/
    }
}
